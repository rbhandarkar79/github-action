name: workflow runner

on:
  workflow_dispatch: 

jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: show OS
        run: |
          echo "this job is running on ubuntu runner"
          echo "this is running on OS: $RUNNER_OS"
  window_echO:
    runs-on: windows-latest 
    steps:
      - name: Show OS
        shell: bash
        run: |
          echo: "this job is running on: $RUNNER_OS"
  


